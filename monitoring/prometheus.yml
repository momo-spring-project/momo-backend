global:
  scrape_interval: 15s

# metric scrape 할 위치 설정
scrape_configs:
  - job_name: 'otel-collector'
    static_configs:
      - targets: ['${PROMETHEUS_OTEL_COLLECTOR_ENDPOINT}']

  - job_name: 'node_exporter'
    static_configs:
      - targets: ['${PROMETHEUS_NODE_EXPORTER_ENDPOINT}']